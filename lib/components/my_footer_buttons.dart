import 'package:flutter/material.dart';

class MyFooterButtons extends StatelessWidget {
  const MyFooterButtons({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
